Simple poc to demonstrate uses of Filters in Angular.

	Basic Filers:
		currency, nmber , date , lowercase, uppercase


		Syntax- Use the | and : characters
			place pipe in expression  e.g. {{artist.name|uppercase}} 
			
			and if you want to give parameter then use colon 
				| limitTo:qty:start

