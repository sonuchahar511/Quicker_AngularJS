simple poc for usecase of 
	
	ng-if : create and destroy some tag in html if condition is true.
	ng-show : it can show some tag if condition is true.
	ng-hide

	Angular generally do this by adding classes to tag.

	ng-repeat